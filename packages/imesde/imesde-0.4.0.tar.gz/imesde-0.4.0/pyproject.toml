[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "imesde"
version = "0.4.0"
description = "Python bindings for imesde engine"
requires-python = ">=3.10"

[tool]

[tool.maturin]
manifest-path = "bindings/python/Cargo.toml"
