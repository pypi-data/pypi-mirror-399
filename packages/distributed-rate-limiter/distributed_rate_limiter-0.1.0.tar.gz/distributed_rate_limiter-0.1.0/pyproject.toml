[project]
name = "distributed-rate-limiter"
version = "0.1.0"
description = "Redis-backed, horizontally scalable rate limiting for distributed systems"
requires-python = ">=3.8"
dependencies = [
    "redis>=4.5"
]
