[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "lismr-delo-js"
version = "0.0.1"
description = "Test package for PyPI publishing"
authors = [{ name = "lismrdelo" }]
requires-python = ">=3.8"
