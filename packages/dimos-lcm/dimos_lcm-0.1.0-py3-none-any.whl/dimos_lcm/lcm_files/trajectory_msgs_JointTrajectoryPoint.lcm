package trajectory_msgs;

struct JointTrajectoryPoint {
    int32_t positions_length;
    int32_t velocities_length;
    int32_t accelerations_length;
    int32_t effort_length;
    double positions[positions_length];
    double velocities[velocities_length];
    double accelerations[accelerations_length];
    double effort[effort_length];
    std_msgs.Duration time_from_start;
}