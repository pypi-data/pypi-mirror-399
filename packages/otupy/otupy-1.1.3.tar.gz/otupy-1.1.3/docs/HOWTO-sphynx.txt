- Copy Makefile and make.bat from test folder
- Install sphinx and its themes:
# pip install -U sphinx
# pip install sphinx-rtd-theme
- Create the build folder, if it does not exist yet
- Run make:
# make
- Start a local webserver from build/html/:
# python3 -m http.server 8888
