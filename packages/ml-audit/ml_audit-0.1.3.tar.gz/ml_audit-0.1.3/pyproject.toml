[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ml-audit"
version = "0.1.3"
description = "Solves Data Lineage Blindness by tracking granular preprocessing steps unlike standard experiment trackers."
readme = "README.md"
license = {file = "LICENSE"}
authors = [{name = "Shivogo John"}]
requires-python = ">=3.9"

dependencies = [
    "numpy",
    "pandas",
    "matplotlib",
    "scikit-learn"
]

[tool.setuptools.packages.find]
where = ["src"]
