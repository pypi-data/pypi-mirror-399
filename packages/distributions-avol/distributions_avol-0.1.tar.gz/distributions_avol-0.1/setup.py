from setuptools import setup

setup(
    name="distributions_avol",
    version="0.1",
    description="Gaussian distributions",
    packages=["distributions"],
    zip_safe=False,
    install_requires=[
        'matplotlib'
    ]
)
