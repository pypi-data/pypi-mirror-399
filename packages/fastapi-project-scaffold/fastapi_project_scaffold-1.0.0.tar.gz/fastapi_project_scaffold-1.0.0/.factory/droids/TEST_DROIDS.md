# Droid 测试指南

**版本**: v1.0.0  
**创建日期**: 2026-01-01

---

## 🎯 测试目标

验证 scaffold-generator 和 module-generator Droid 的功能完整性和代码质量。

---

## 📋 测试清单

### scaffold-generator Droid

```yaml
基础功能:
  - [ ] 简单需求解析（2 个实体）
  - [ ] 复杂需求解析（3+ 个实体）
  - [ ] 关系推断（外键识别）
  - [ ] 项目初始化
  - [ ] 模块生成
  - [ ] 路由注册
  - [ ] 数据库初始化

智能推断:
  - [ ] 字段类型推断
  - [ ] 必需性推断
  - [ ] 约束推断
  - [ ] 关系类型推断

代码质量:
  - [ ] 生成代码语法正确
  - [ ] 类型提示完整
  - [ ] 无导入错误
  - [ ] 符合项目规范

集成测试:
  - [ ] 服务器启动成功
  - [ ] API 文档可访问
  - [ ] 所有端点正常
  - [ ] 数据库操作正常

迭代修复:
  - [ ] 错误检测准确
  - [ ] 自动修复成功
  - [ ] 验证通过
```

### module-generator Droid

```yaml
基础功能:
  - [ ] 环境检查
  - [ ] 字段推断
  - [ ] 代码生成
  - [ ] 关系处理
  - [ ] 约束增强
  - [ ] 自动集成

智能推断:
  - [ ] 11 种字段类型
  - [ ] 必需性判断
  - [ ] 约束条件解析
  - [ ] 外键识别
  - [ ] 关系类型判断

代码质量:
  - [ ] Model 正确
  - [ ] Schema 完整
  - [ ] CRUD 完整
  - [ ] API 完整
  - [ ] 类型安全

集成状态:
  - [ ] models/__init__.py 更新
  - [ ] main.py 路由注册
  - [ ] README.md 更新

功能测试:
  - [ ] 语法检查通过
  - [ ] 导入测试通过
  - [ ] API 测试通过

迭代修复:
  - [ ] CheckList 验证准确
  - [ ] 错误修复有效
  - [ ] 迭代收敛
```

---

## 🧪 测试用例

### 测试用例 1: scaffold-generator - 博客系统

**输入**:
```yaml
description: |
  创建一个博客系统，包含文章、评论和标签。
  文章有标题、内容、作者、发布状态。
  评论关联到文章，包含内容和作者。
  标签可以关联多篇文章。
project_name: test-blog
database: sqlite
auth: true
include_examples: false
```

**预期输出**:
```yaml
实体数: 3
  - Article
  - Comment
  - Tag

关系:
  - Comment.article_id → Article
  - Article.author_id → User
  - Comment.author_id → User
  - Article ↔ Tag (many_to_many)

文件数: 13+
  - 3 个 Model
  - 3 个 Schema
  - 3 个 CRUD
  - 3 个 API

API 端点: 15
数据表: 4 (3 实体 + 1 关联表)

验证:
  ✅ 项目目录创建
  ✅ 所有文件生成
  ✅ 路由注册完成
  ✅ 数据库初始化成功
  ✅ 服务器启动正常
  ✅ API 端点可访问
```

**测试命令**:
```bash
# 1. 调用 Droid（在 Factory 界面）
# 2. 验证生成结果
cd test-blog
ls -la app/models/
ls -la app/schemas/
ls -la app/crud/
ls -la app/api/v1/

# 3. 检查文件内容
cat app/models/article.py
cat app/schemas/comment.py

# 4. 语法检查
python -m py_compile app/**/*.py

# 5. 导入测试
python -c "from app.main import app; print('OK')"

# 6. 启动测试
uvicorn app.main:app --reload &
sleep 3

# 7. API 测试
curl http://localhost:8000/docs
curl http://localhost:8000/api/v1/articles

# 8. 清理
pkill -f uvicorn
cd ..
rm -rf test-blog
```

---

### 测试用例 2: scaffold-generator - 电商系统

**输入**:
```yaml
description: |
  电商后台系统，需要商品、订单和库存管理。
  商品包含名称、价格、库存、分类、描述。
  订单包含订单号、用户、商品、数量、总价、状态。
  库存记录包含商品、仓库、数量、更新时间。
project_name: test-shop
database: sqlite
auth: true
```

**预期输出**:
```yaml
实体数: 3
  - Product
  - Order
  - Stock

关系:
  - Order.user_id → User
  - Order.product_id → Product
  - Stock.product_id → Product

特殊字段:
  - Product.price: float, ge=0
  - Order.order_code: str, unique, pattern
  - Order.status: enum
  - Stock.quantity: int, ge=0

验证重点:
  ✅ 枚举类型生成
  ✅ 唯一约束添加
  ✅ 数值约束正确
  ✅ 关系双向配置
```

---

### 测试用例 3: module-generator - 文章模块

**输入**:
```yaml
description: |
  文章模块，包含标题（必需，最多200字符）、
  内容（必需，长文本）、作者（关联用户表）、
  发布状态（布尔，默认false）、发布时间（可选）。
module_name: article
with_api: true
auto_register: true
```

**预期输出**:
```yaml
字段推断:
  - title: str, required, max_length=200
  - content: text, required
  - author_id: int, required, ForeignKey("users.id")
  - published: bool, required, default=false
  - published_at: datetime, optional

文件生成:
  ✅ app/models/article.py
  ✅ app/schemas/article.py
  ✅ app/crud/article.py
  ✅ app/api/v1/articles.py

关系处理:
  ✅ ForeignKey 定义
  ✅ relationship 添加
  ✅ User Model 更新

集成状态:
  ✅ models/__init__.py 更新
  ✅ main.py 路由注册
  ✅ README.md 更新

验证:
  ✅ 语法正确
  ✅ 导入成功
  ✅ API 可访问
  ✅ CRUD 操作正常
```

**测试命令**:
```bash
# 1. 创建测试项目
cd fastapi-scaffold
python cli/main.py init test-module

# 2. 调用 Droid 生成模块

# 3. 验证文件
cd test-module
ls app/models/article.py
ls app/schemas/article.py
ls app/crud/article.py
ls app/api/v1/articles.py

# 4. 检查集成
grep "article" app/models/__init__.py
grep "articles" app/main.py

# 5. 语法检查
python -m py_compile app/models/article.py

# 6. 导入测试
python -c "from app.models.article import Article"

# 7. API 测试
uvicorn app.main:app --reload &
sleep 3
curl http://localhost:8000/api/v1/articles

# 8. 清理
pkill -f uvicorn
cd ..
rm -rf test-module
```

---

### 测试用例 4: module-generator - 复杂约束

**输入**:
```yaml
description: |
  订单模块，订单号唯一（格式ORD+8位数字），
  用户关联用户表，总金额大于0，
  状态枚举（待支付、已支付、已发货、已完成、已取消），
  备注可选（最多500字）。
module_name: order
with_api: true
auto_register: true
```

**预期输出**:
```yaml
字段推断:
  - order_code: str, unique, pattern=r"^ORD[0-9]{8}$"
  - user_id: int, ForeignKey("users.id")
  - total_amount: float, ge=0
  - status: OrderStatus (enum)
  - remark: text, optional, max_length=500

特殊处理:
  ✅ 唯一约束
  ✅ Pattern 验证
  ✅ 枚举类定义
  ✅ 外键关系
  ✅ 长度约束

验证重点:
  ✅ Model 唯一约束
  ✅ Schema Pattern 验证
  ✅ Enum 类生成
  ✅ 关系双向配置
```

---

### 测试用例 5: module-generator - 多关系

**输入**:
```yaml
description: |
  任务模块，标题必需，描述可选，
  创建人关联用户表，负责人关联用户表，
  项目关联项目表，优先级枚举（低、中、高），
  状态枚举（待办、进行中、已完成），截止日期可选。
module_name: task
```

**预期输出**:
```yaml
关系推断:
  - created_by_id → User (creator)
  - assigned_to_id → User (assignee)
  - project_id → Project

枚举推断:
  - TaskPriority: low, medium, high
  - TaskStatus: todo, in_progress, completed

验证重点:
  ✅ 多个外键到同一表
  ✅ 不同 back_populates 名称
  ✅ 多个枚举类
  ✅ 关系命名清晰
```

---

## 📊 性能测试

### 测试场景 1: 单实体生成

```yaml
输入: 1 个实体，5 个字段
预期时间: < 30 秒
预期文件: 4 个

验证:
  - 生成时间
  - 代码质量
  - CheckList 通过率
```

### 测试场景 2: 多实体生成

```yaml
输入: 3 个实体，每个 5-8 个字段
预期时间: < 90 秒
预期文件: 13+ 个

验证:
  - 生成时间
  - 并发处理
  - 集成完整性
```

### 测试场景 3: 复杂关系

```yaml
输入: 2 个实体，包含多对多关系
预期时间: < 60 秒
预期文件: 9+ 个（含关联表）

验证:
  - 关系识别准确度
  - 关联表生成
  - 双向配置正确
```

---

## 🔍 质量指标

### 代码质量

```yaml
语法正确率: 100%
  - 所有生成文件无语法错误

类型安全: 100%
  - 完整类型提示
  - mypy 检查通过

导入正确率: 100%
  - 无循环依赖
  - 所有导入有效

规范遵循: 100%
  - 符合项目规范
  - 命名一致性
  - 代码风格统一
```

### 功能完整性

```yaml
CheckList 通过率: >= 90%
  - 第一次生成通过率

迭代修复成功率: >= 95%
  - 3 次迭代内修复

集成成功率: 100%
  - 路由注册
  - 模块导入
  - 数据库迁移
```

### 智能推断准确度

```yaml
字段类型: >= 95%
  - 正确识别字段类型

必需性: >= 90%
  - 正确判断必需/可选

约束条件: >= 85%
  - 正确推断约束

关系识别: >= 90%
  - 正确识别外键和关系类型
```

---

## 🐛 错误测试

### 边界情况

```yaml
测试 1: 空描述
  输入: ""
  预期: 错误提示，要求提供描述

测试 2: 模糊描述
  输入: "做个系统"
  预期: 错误提示，要求更详细描述

测试 3: 不存在的关联表
  输入: 关联到不存在的表
  预期: 错误提示或跳过关系定义

测试 4: 重复模块名
  输入: 已存在的模块名
  预期: 询问是否覆盖
```

### 错误恢复

```yaml
测试 1: CLI 命令失败
  场景: cli/main.py 路径错误
  预期: 清晰错误提示，建议解决方案

测试 2: 导入错误
  场景: 循环依赖
  预期: 检测并调整导入顺序

测试 3: 数据库错误
  场景: 字段名是 SQL 关键字
  预期: 自动添加引号或重命名
```

---

## 📋 测试报告模板

```markdown
# Droid 测试报告

**测试日期**: 2026-01-01  
**测试人员**: XXX  
**Droid 版本**: v1.0.0

## 测试环境
- Python: 3.10+
- FastAPI: 0.104+
- 操作系统: Windows/Linux/Mac

## 测试结果

### scaffold-generator

| 测试用例 | 状态 | 耗时 | 备注 |
|---------|------|------|------|
| 博客系统 | ✅ | 45s | 3 实体，15 端点 |
| 电商系统 | ✅ | 60s | 关系正确 |
| ... | | | |

**通过率**: XX/XX (XX%)

### module-generator

| 测试用例 | 状态 | 耗时 | 备注 |
|---------|------|------|------|
| 文章模块 | ✅ | 20s | 字段推断准确 |
| 订单模块 | ✅ | 25s | 约束完整 |
| ... | | | |

**通过率**: XX/XX (XX%)

## 质量指标

- 代码语法正确率: XX%
- CheckList 通过率: XX%
- 智能推断准确度: XX%

## 发现的问题

1. XXX
2. XXX

## 改进建议

1. XXX
2. XXX
```

---

## 🎓 测试最佳实践

### 测试前准备

```bash
# 1. 备份现有项目
cp -r fastapi-scaffold fastapi-scaffold.backup

# 2. 清理测试项目
rm -rf test-*

# 3. 确认环境
python --version
python -m pip list | grep fastapi
```

### 测试中监控

```bash
# 监控文件生成
watch -n 1 'ls -la test-*/app/models/'

# 监控进程
ps aux | grep droid

# 监控日志
tail -f test-*.log
```

### 测试后清理

```bash
# 停止所有服务
pkill -f uvicorn

# 清理测试项目
rm -rf test-*

# 恢复备份（如需要）
rm -rf fastapi-scaffold
mv fastapi-scaffold.backup fastapi-scaffold
```

---

**维护者**: 项目团队  
**版本**: v1.0.0  
**创建日期**: 2026-01-01
