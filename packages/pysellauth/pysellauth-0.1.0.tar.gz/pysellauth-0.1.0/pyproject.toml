[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pysellauth"
version = "0.1.0"
description = "Python wrapper for the SellAuth API"
authors = [{name = "Your Name"}]
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}

dependencies = [
    "requests>=2.0.0"
]

[project.urls]
Homepage = "https://github.com/yourname/pysellauth"
