# Raven 🐦‍⬛

Be Odin. Send ravens.

Raven is a semantic CLI wrapper around Git that replaces mechanical commands
with intent-driven verbs like `watch`, `gather`, and `fly`.

Built for fun. Built to feel good to use.
