[project]
name = "agentscale"
version = "0.1.0"
description = "AgentScale an open source control plane for generative AI agents"
readme = "README.md"
authors = [
    { name = "Qiulin Li", email = "liqiulin@outlook.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
agentscale = "agentscale:main"

[build-system]
requires = ["uv_build>=0.8.15,<0.9.0"]
build-backend = "uv_build"
