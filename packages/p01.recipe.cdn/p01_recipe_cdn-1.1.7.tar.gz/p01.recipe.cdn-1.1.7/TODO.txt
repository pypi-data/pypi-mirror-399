====
TODO
====

- migrate simit to calmjs because of instable python 3 version

- remove old svn file version from svn if we add a new version if we extract per
  svn and package version

- remove old package file version from svn if we add a new file version if we
  extract per svn and package version
