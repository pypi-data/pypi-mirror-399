name: DCO Check

on:
  pull_request:
    branches: [main, develop]

jobs:
  dco:
    name: Developer Certificate of Origin
    runs-on: ubuntu-latest
    steps:
      - name: Check DCO
        uses: dblock/dco-check@v2
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
