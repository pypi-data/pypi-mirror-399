[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "eap2api"
version = "0.1.0"
authors = [
  { name="Wuaze", email="jorgehoyaprado1@gmail.com" }
]
description = "Library for make tkinter easier"
readme = "README.md"
requires-python = ">=3.9"
