[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rpi-lcd-i2c-animations"
version = "0.1.0"
description = "LCD animation add-ons for rpi_lcd_i2c"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [
  { name = "Sammy" }
]

dependencies = [
  "rpi-lcd-i2c"
]

[project.urls]
Homepage = "https://github.com/Sam67-coder/rpi_lcd_i2c_animations"
Repository = "https://github.com/Sam67-coder/rpi_lcd_i2c_animations"

