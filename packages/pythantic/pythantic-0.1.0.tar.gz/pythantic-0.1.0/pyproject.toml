[project]
name = "pythantic"
version = "0.1.0"
description = "heise plz don't typosquat your own reader"
readme = "README.md"
authors = [
    { name = "konstin", email = "konstin@mailbox.org" }
]
requires-python = ">=3.9"
dependencies = [
]

[build-system]
requires = ["uv_build>=0.9.18,<0.10.0"]
build-backend = "pythantic.build_backend"
backend-path = ["src"]
