[tool.poetry]
name    = "satcube"
version = "0.1.20"
description = "A Python package to create cloud-free monthly composites by fusing Landsat and Sentinel-2 data."
authors = ["Cesar Aybar <fcesar.aybar@uv.es>"]
repository     = "https://github.com/IPL-UV/satcube"
documentation  = "https://ipl-uv.github.io/satcube/"
readme         = "README.md"
packages       = [{ include = "satcube" }]

[tool.poetry.dependencies]
python            = ">=3.9"
cubexpress      = "0.1.24"
mlstac          = ">=0.4.0"
satalign        = "0.1.13"         
segmentation-models-pytorch = ">=0.3.0"
phicloudmask    = ">=0.0.2" 
scikit-learn    = ">=1.2.0"
requests        = ">=2.26.0"
xarray          = ">=2023.7.0"  
tqdm            = ">=4.67.1"

[tool.poetry.extras]
full = ["torch"]

# --- tooling fixes ----------------------------------------------------------
[tool.mypy]
files                   = ["satcube"]
disallow_untyped_defs   = true
disallow_any_unimported = true
no_implicit_optional    = true
check_untyped_defs      = true
warn_return_any         = true
warn_unused_ignores     = true
show_error_codes        = true

[tool.ruff]
target-version = "py310"
line-length    = 120
fix            = true
select         = ["YTT","S","B","A","C4","T10","SIM","I","C90","E","W","F","PGH","UP","RUF","TRY"]
ignore = [
    "E501","E731",
    "RUF002",  # Docstring ambiguous characters (×, –)
    "SIM108",  # Ternary operator suggestions
    "RUF059",  # Unused unpacked variables
    "TRY003",  # Long exception messages
    "C901",    # Complex functions (legítimamente complejas)
]