[project]
name = "ecrire"          
version = "0.1.0"
description = "fonction ecrire au lieu de print"
authors = [{name="Adam Joliclerc"}]
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
