[project]
name = "my-mcp-project-mysql-last-push"
version = "0.1.1"   # 每次 升级版本号
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "张旭东", email = "zhang_brz@163.com" }
]
requires-python = ">=3.14.2"
dependencies = [
    "mcp[cli]>=1.25.0",
]

[project.scripts]
my-mcp-project-mysql-last-push = "my_mcp_project_mysql_last_push:main"

[build-system]
requires = ["uv_build>=0.9.18,<0.10.0"]
build-backend = "uv_build"
