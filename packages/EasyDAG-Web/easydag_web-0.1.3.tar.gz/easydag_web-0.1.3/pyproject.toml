[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "EasyDAG-Web"
version = "0.1.3"
description = "A small EasyDAG extension with predefined websocket setup for API usage."
readme = "README.md"
requires-python = ">=3.9"

authors = [
    {name = "Mechatronicist"}
]

license = {file = "LICENSE"}
keywords = ["dag", "multiprocessing", "workflow", "parallel", "tasks", "web", "api"]

dependencies = [
    "EasyDAG"
]

[project.urls]
Homepage = "https://github.com/mechatronicist/EasyDAG-Web"
Repository = "https://github.com/mechatronicist/EasyDAG-Web"
Issues = "https://github.com/mechatronicist/EasyDAG-Web/issues"
