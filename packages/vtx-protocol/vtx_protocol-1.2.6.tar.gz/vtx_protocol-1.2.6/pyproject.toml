[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "vtx-protocol"
version = "1.2.6"
description = "WIT definitions for VTX plugins"
readme = "README.md"
requires-python = ">=3.8"

[tool.setuptools.package-data]
vtx_protocol = ["wit/*.wit"]