[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rocinet"
version = "0.4.0"
description = "Rocinet SDK package for Python"
readme = "README.md"
authors = [{ name = "Michal Bartl", email = "michal.bartl@gmail.com" }]
license = { text = "WTFPL" }
dependencies = ["requests"]
