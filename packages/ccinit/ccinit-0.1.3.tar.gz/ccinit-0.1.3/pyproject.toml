[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ccinit"
version = "0.1.3"
description = "Fetches and inlines remote C include dependencies"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.8"
authors = [
  { name = "Gabriel Belão Kaminski" }
]

[project.scripts]
ccinit = "ccinit:main"
