[project]
name = "bunshin"
version = "0.0.0"
requires-python = ">=3.11"
readme = "README.md"
dependencies = []
authors = [{ name = "Sébastien De Revière", email = "contact@sderev.com" }]

[build-system]
requires = ["uv_build>=0.9.21,<0.10.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "bunshin"
module-root = ""
