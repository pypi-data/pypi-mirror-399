[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "llamaindex-primordia"
version = "0.1.0"
description = "Primordia metering for LlamaIndex"
readme = "README.md"
license = "MIT"
requires-python = ">=3.9"
dependencies = ["llama-index-core>=0.10.0"]

[project.urls]
Kernel = "https://clearing.kaledge.app"

[tool.hatch.build.targets.wheel]
packages = ["llamaindex_primordia"]
