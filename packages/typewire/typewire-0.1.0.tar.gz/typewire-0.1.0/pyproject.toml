[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "typewire"
version = "0.1.0"
description = "A single-file utility to allow better runtime handling of types by providing a predictable way of transforming data into the shape of a given type hint."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
authors = [
    { name = "Lily Ellington", email = "lilell_@outlook.com " }
]
keywords = [
    "typing",
    "type-hints",
    "type-casting",
    "runtime-types",
    "casting",
    "annotated"
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Typing :: Typed",
]

[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F"]
ignore = ["I001"]

[tool.ruff.isort]
known-local-folder = ["typewire"]
force-single-line = false
lines-after-imports = 1
force-sort-within-sections = true
order-by-type = false

[tool.mypy]
python_version = "3.10"
strict = true

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "tox>=4.32.0",
]

[project.urls]
repository = "https://github.com/lilellia/typewire"
"Bug Tracker" = "https://github.com/lilellia/typewire/issues"

[tool.hatch.build]
include = [
    "typewire",
]
