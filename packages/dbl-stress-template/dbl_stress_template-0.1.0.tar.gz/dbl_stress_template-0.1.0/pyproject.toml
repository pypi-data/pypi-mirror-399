[project]
name = "dbl-stress-template"
version = "0.1.0"
description = "Minimal DBL stress-case runner for deterministic event streams"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
dependencies = [
  "dbl-core>=0.3.2,<0.4.0",
  "pyyaml>=6.0.1",
]
