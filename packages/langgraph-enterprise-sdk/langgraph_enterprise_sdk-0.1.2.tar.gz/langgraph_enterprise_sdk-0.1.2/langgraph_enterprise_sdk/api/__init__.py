"""
Core API layer for the LangGraph Python SDK.

This package defines TOON + ZAD compliant abstractions:
- State (immutable, replayable)
- Node (orchestration only)
- Edge (pure routing)
- Graph (structure only)
- RunnableGraph (deterministic execution)
"""
