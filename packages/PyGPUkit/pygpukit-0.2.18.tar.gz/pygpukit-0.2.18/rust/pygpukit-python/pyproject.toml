[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "pygpukit-rust"
version = "0.2.0"
description = "PyGPUkit Rust bindings"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "_pygpukit_rust"
