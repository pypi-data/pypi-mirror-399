[project]
dependencies = ["pyside6-essentials>=6.0"]
description = "Add your description here"
name = "ex08_pyside6_py313"
readme = "README.md"
requires-python = ">=3.13"
version = "0.1.0"

[build-system]
build-backend = "poetry.core.masonry.api"
requires = ["poetry-core"]
