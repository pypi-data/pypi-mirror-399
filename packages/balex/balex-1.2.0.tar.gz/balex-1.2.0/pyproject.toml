[project]
name = "balex"
version = "1.2.0"
description = "Python library Bale Messenger"
authors = [{name="z8r-service"}]
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["aiohttp"]

[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"
