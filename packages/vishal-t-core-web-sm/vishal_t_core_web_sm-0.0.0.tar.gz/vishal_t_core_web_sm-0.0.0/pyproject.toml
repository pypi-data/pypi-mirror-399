[build-vishal-t-core-web-smtem]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "vishal-t-core-web-sm"
version = "0.0.0"
description = "Dummy vishal-t-core-web-sm package for PyPI"
authors = [{name="Your Name", email="you@example.com"}]
readme = "README.md"
requires-python = ">=3.7"

[tool.setuptools.packages.find]
where = ["src"]
