# {{ cookiecutter.project_name }}

> {{ cookiecutter.project_description }}

## Overview

{{ cookiecutter.project_name }} provides {{ cookiecutter.main_functionality }}. {{ cookiecutter.additional_description }}

- **Package**: `pip install {{ cookiecutter.package_name }}`
- **Python**: {{ cookiecutter.python_version }}+
- **Platforms**: {{ cookiecutter.platforms }}
- **License**: {{ cookiecutter.license }}

## Quick Start

### Installation

```bash
# Basic installation
pip install {{ cookiecutter.package_name }}

# With optional dependencies
pip install {{ cookiecutter.package_name }}[optional]
```

### Basic Usage

```python
from {{ cookiecutter.package_name }} import {{ cookiecutter.main_class }}

# Create instance
instance = {{ cookiecutter.main_class }}.create("My App", config={...})
instance.initialize()
```

## Core API Reference

### {{ cookiecutter.main_class }} Class

The main class for {{ cookiecutter.main_functionality }}.

#### Constructor Parameters

```python
{{ cookiecutter.main_class }}(
    param1: type = default,      # Description
    param2: type = default,       # Description
    param3: type = None,          # Optional description
)
```

#### Factory Methods

```python
# Recommended way to create instance
instance = {{ cookiecutter.main_class }}.create(
    name="My Instance",
    config={...},
    option=True,
)

# Alternative creation method
instance = {{ cookiecutter.main_class }}.create_alternative(
    param=value,
)
```

#### Core Methods

```python
# Primary operation
instance.primary_method(param1, param2)

# Secondary operation
result = instance.secondary_method(param)

# Async operation
await instance.async_method(param)
```

#### Properties

```python
instance.property1          # Get/set property
instance.property2          # Read-only property
instance.property3          # Computed property
```

### Python → External Communication

```python
# Emit event
instance.emit("event_name", {"key": "value"})
```

```javascript
// External: Listen for events
external.on('event_name', (data) => {
    console.log('Received:', data);
});
```

### External → Python Communication

#### Method 1: Events (fire-and-forget)

```javascript
// External: Send event
external.send_event('action_name', {
    param: 'value'
});
```

```python
# Python: Register event handler
@instance.on("action_name")
def handle_action(data):
    print(f"Action: {data['param']}")
```

#### Method 2: RPC with Return Value

```javascript
// External: Call Python method and get result
const result = await external.api.method_name({ param: value });
console.log(result);
```

```python
# Python: Bind callable method
@instance.bind_call("api.method_name")
def method_name(param=None):
    return {"result": "value"}
```

### API Binding

```python
# Bind single method
@instance.bind_call("api.method")
def method(param: str = "") -> dict:
    return {"result": param}

# Bind all methods of an object
class API:
    def get_data(self) -> dict:
        return {"items": [1, 2, 3]}

    def save_item(self, name: str = "", value: int = 0) -> dict:
        return {"ok": True, "name": name}

instance.bind_api(API(), namespace="api")
```

### Event Handling

```python
@instance.on_event
def on_event(data):
    print("Event received:", data)

@instance.on_custom_event
def on_custom_event(data):
    return True  # Return True to allow, False to cancel
```

### Error Handling

```python
# Handle errors
try:
    result = instance.operation()
except CustomError as e:
    print(f"Error: {e}")
    # Handle error
```

## Advanced Features

### Feature 1: Advanced Configuration

```python
# Configure advanced options
instance.configure(
    option1=True,
    option2="value",
    option3={"nested": "config"}
)
```

### Feature 2: Integration Support

```python
# Integration with external systems
from {{ cookiecutter.package_name }}.integration import ExternalIntegration

integration = ExternalIntegration(
    system="external_system",
    config={...}
)
integration.connect()
```

### Feature 3: Standalone Mode

```python
from {{ cookiecutter.package_name }} import run_standalone

# One-liner for standalone usage
run_standalone(
    name="My App",
    config={...},
    option=True
)
```

## Command Line Interface

```bash
# Basic usage
{{ cookiecutter.package_name }} --config config.yaml

# With options
{{ cookiecutter.package_name }} --config config.yaml --option value

# Using uvx
uvx {{ cookiecutter.package_name }} --help
```

## Testing Framework

### Test Utilities

```python
from {{ cookiecutter.package_name }}.testing import TestUtilities

# Create test instance
with TestUtilities.create_test_instance() as instance:
    # Run tests
    result = instance.test_method()
    assert result == expected
```

## Platform Support

| Platform | Status | Python | Notes |
|----------|--------|--------|-------|
| Windows | ✅ | {{ cookiecutter.python_version }}+ | Full support |
| macOS | ✅ | {{ cookiecutter.python_version }}+ | Full support |
| Linux | ✅ | {{ cookiecutter.python_version }}+ | Full support |

## JavaScript API Reference (if applicable)

### window.{{ cookiecutter.package_name }}

```javascript
// Call Python method (returns Promise)
const result = await {{ cookiecutter.package_name }}.call('api.method_name', { param: value });

// Call via api proxy (shorthand)
const result = await {{ cookiecutter.package_name }}.api.method_name({ param: value });

// Send event to Python (fire-and-forget)
{{ cookiecutter.package_name }}.send_event('event_name', { data: value });

// Listen for Python events
const unsubscribe = {{ cookiecutter.package_name }}.on('event_name', (data) => {
    console.log('Received:', data);
});

// Unsubscribe
{{ cookiecutter.package_name }}.off('event_name', handler);
```

### Ready Event

```javascript
window.addEventListener('{{ cookiecutter.package_name }}ready', () => {
    // Safe to use {{ cookiecutter.package_name }} API
    {{ cookiecutter.package_name }}.api.init();
});
```

## Key Imports

```python
from {{ cookiecutter.package_name }} import (
    # Core
    {{ cookiecutter.main_class }},
    {{ cookiecutter.utility_class }},

    # Integration
    {{ cookiecutter.integration_class }},
    run_standalone,

    # Utilities
    {{ cookiecutter.utility_function }},
)
```

## Examples

### Example 1: Basic Usage

```python
from {{ cookiecutter.package_name }} import {{ cookiecutter.main_class }}

# Create and use
instance = {{ cookiecutter.main_class }}.create("Example")
instance.initialize()
result = instance.process()
print(result)
```

### Example 2: Advanced Usage

```python
from {{ cookiecutter.package_name }} import {{ cookiecutter.main_class }}

# Advanced configuration
instance = {{ cookiecutter.main_class }}.create(
    name="Advanced Example",
    config={
        "option1": True,
        "option2": "value"
    }
)

# Register handlers
@instance.on("event")
def handler(data):
    print(f"Event: {data}")

# Use
instance.start()
```

## Links

- **Repository**: https://github.com/{{ cookiecutter.github_username }}/{{ cookiecutter.package_name }}
- **PyPI**: https://pypi.org/project/{{ cookiecutter.package_name }}/
- **Documentation**: https://github.com/{{ cookiecutter.github_username }}/{{ cookiecutter.package_name }}/tree/main/docs
- **Examples**: https://github.com/{{ cookiecutter.github_username }}/{{ cookiecutter.package_name }}/tree/main/examples

