# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import typing

import httpx
from .core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from .environment import RestClientEnvironment

if typing.TYPE_CHECKING:
    from .agent_api.client import AgentApiClient, AsyncAgentApiClient
    from .human_api.client import AsyncHumanApiClient, HumanApiClient
    from .system.client import AsyncSystemClient, SystemClient
    from .test.client import AsyncTestClient, TestClient


class RestClient:
    """
    Use this class to access the different functions within the SDK. You can instantiate any number of clients with different configuration that will propagate to these functions.

    Parameters
    ----------
    base_url : typing.Optional[str]
        The base url to use for requests from the client.

    environment : RestClientEnvironment
        The environment to use for requests from the client. from .environment import RestClientEnvironment



        Defaults to RestClientEnvironment.DEFAULT



    api_key : str
    headers : typing.Optional[typing.Dict[str, str]]
        Additional headers to send with every request.

    timeout : typing.Optional[float]
        The timeout to be used, in seconds, for requests. By default the timeout is 60 seconds, unless a custom httpx client is used, in which case this default is not enforced.

    follow_redirects : typing.Optional[bool]
        Whether the default httpx client follows redirects or not, this is irrelevant if a custom httpx client is passed in.

    httpx_client : typing.Optional[httpx.Client]
        The httpx client to use for making requests, a preconfigured client is used by default, however this is useful should you want to pass in any custom httpx configuration.

    Examples
    --------
    from thenvoi_rest import RestClient

    client = RestClient(
        api_key="YOUR_API_KEY",
    )
    """

    def __init__(
        self,
        *,
        base_url: typing.Optional[str] = None,
        environment: RestClientEnvironment = RestClientEnvironment.DEFAULT,
        api_key: str,
        headers: typing.Optional[typing.Dict[str, str]] = None,
        timeout: typing.Optional[float] = None,
        follow_redirects: typing.Optional[bool] = True,
        httpx_client: typing.Optional[httpx.Client] = None,
    ):
        _defaulted_timeout = (
            timeout if timeout is not None else 60 if httpx_client is None else httpx_client.timeout.read
        )
        self._client_wrapper = SyncClientWrapper(
            base_url=_get_base_url(base_url=base_url, environment=environment),
            api_key=api_key,
            headers=headers,
            httpx_client=httpx_client
            if httpx_client is not None
            else httpx.Client(timeout=_defaulted_timeout, follow_redirects=follow_redirects)
            if follow_redirects is not None
            else httpx.Client(timeout=_defaulted_timeout),
            timeout=_defaulted_timeout,
        )
        self._agent_api: typing.Optional[AgentApiClient] = None
        self._system: typing.Optional[SystemClient] = None
        self._human_api: typing.Optional[HumanApiClient] = None
        self._test: typing.Optional[TestClient] = None

    @property
    def agent_api(self):
        if self._agent_api is None:
            from .agent_api.client import AgentApiClient  # noqa: E402

            self._agent_api = AgentApiClient(client_wrapper=self._client_wrapper)
        return self._agent_api

    @property
    def system(self):
        if self._system is None:
            from .system.client import SystemClient  # noqa: E402

            self._system = SystemClient(client_wrapper=self._client_wrapper)
        return self._system

    @property
    def human_api(self):
        if self._human_api is None:
            from .human_api.client import HumanApiClient  # noqa: E402

            self._human_api = HumanApiClient(client_wrapper=self._client_wrapper)
        return self._human_api

    @property
    def test(self):
        if self._test is None:
            from .test.client import TestClient  # noqa: E402

            self._test = TestClient(client_wrapper=self._client_wrapper)
        return self._test


class AsyncRestClient:
    """
    Use this class to access the different functions within the SDK. You can instantiate any number of clients with different configuration that will propagate to these functions.

    Parameters
    ----------
    base_url : typing.Optional[str]
        The base url to use for requests from the client.

    environment : RestClientEnvironment
        The environment to use for requests from the client. from .environment import RestClientEnvironment



        Defaults to RestClientEnvironment.DEFAULT



    api_key : str
    headers : typing.Optional[typing.Dict[str, str]]
        Additional headers to send with every request.

    timeout : typing.Optional[float]
        The timeout to be used, in seconds, for requests. By default the timeout is 60 seconds, unless a custom httpx client is used, in which case this default is not enforced.

    follow_redirects : typing.Optional[bool]
        Whether the default httpx client follows redirects or not, this is irrelevant if a custom httpx client is passed in.

    httpx_client : typing.Optional[httpx.AsyncClient]
        The httpx client to use for making requests, a preconfigured client is used by default, however this is useful should you want to pass in any custom httpx configuration.

    Examples
    --------
    from thenvoi_rest import AsyncRestClient

    client = AsyncRestClient(
        api_key="YOUR_API_KEY",
    )
    """

    def __init__(
        self,
        *,
        base_url: typing.Optional[str] = None,
        environment: RestClientEnvironment = RestClientEnvironment.DEFAULT,
        api_key: str,
        headers: typing.Optional[typing.Dict[str, str]] = None,
        timeout: typing.Optional[float] = None,
        follow_redirects: typing.Optional[bool] = True,
        httpx_client: typing.Optional[httpx.AsyncClient] = None,
    ):
        _defaulted_timeout = (
            timeout if timeout is not None else 60 if httpx_client is None else httpx_client.timeout.read
        )
        self._client_wrapper = AsyncClientWrapper(
            base_url=_get_base_url(base_url=base_url, environment=environment),
            api_key=api_key,
            headers=headers,
            httpx_client=httpx_client
            if httpx_client is not None
            else httpx.AsyncClient(timeout=_defaulted_timeout, follow_redirects=follow_redirects)
            if follow_redirects is not None
            else httpx.AsyncClient(timeout=_defaulted_timeout),
            timeout=_defaulted_timeout,
        )
        self._agent_api: typing.Optional[AsyncAgentApiClient] = None
        self._system: typing.Optional[AsyncSystemClient] = None
        self._human_api: typing.Optional[AsyncHumanApiClient] = None
        self._test: typing.Optional[AsyncTestClient] = None

    @property
    def agent_api(self):
        if self._agent_api is None:
            from .agent_api.client import AsyncAgentApiClient  # noqa: E402

            self._agent_api = AsyncAgentApiClient(client_wrapper=self._client_wrapper)
        return self._agent_api

    @property
    def system(self):
        if self._system is None:
            from .system.client import AsyncSystemClient  # noqa: E402

            self._system = AsyncSystemClient(client_wrapper=self._client_wrapper)
        return self._system

    @property
    def human_api(self):
        if self._human_api is None:
            from .human_api.client import AsyncHumanApiClient  # noqa: E402

            self._human_api = AsyncHumanApiClient(client_wrapper=self._client_wrapper)
        return self._human_api

    @property
    def test(self):
        if self._test is None:
            from .test.client import AsyncTestClient  # noqa: E402

            self._test = AsyncTestClient(client_wrapper=self._client_wrapper)
        return self._test


def _get_base_url(*, base_url: typing.Optional[str] = None, environment: RestClientEnvironment) -> str:
    if base_url is not None:
        return base_url
    elif environment is not None:
        return environment.value
    else:
        raise Exception("Please pass in either base_url or environment to construct the client")
