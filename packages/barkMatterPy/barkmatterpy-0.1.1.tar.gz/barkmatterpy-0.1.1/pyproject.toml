[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "barkMatterPy"
version = "0.1.1"
description = "barkMatterPy compiler / obfuscation engine"
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }

authors = [
    { name = "JACKMB", email = "gskksksj@gmail.com" }
]

classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[project.scripts]
barkMatterPy = "barkMatterPy.__main__:main"