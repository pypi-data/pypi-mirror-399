[build-system]
requires = ["uv_build>=0.8.4,<0.9.0"]
build-backend = "uv_build"

[project]
version = "0.3.0"
 dependencies = [
    "accelerate>=1.9.0",
    "boto3>=1.40.0",
    "botocore[crt]>=1.40.0",
    "chatterbox-tts",                # Custom developer fork/source defined in tool.uv.sources
    "diffusers>=0.34.0",
    "diskcache>=5.6.3",
    "distro>=1.9.0",
    "numba>=0.60.0",                 # floor to 3.12+ compatible version
    "numpy>=1.26.0",                 # floor to 3.12+ compatible version (distutils removed)
    "openai>=1.98.0",
    "peft>=0.16.0,<1.0",
    "platformdirs>=4.3,<5.0",
    "psutil>=7.0,<8.0",
    "pydub>=0.25,<1.0",
    "python-dotenv>=1.1.1",
    "torch>=2.6,<3.0",               # chatterbox restriction, find out why later
    "torchaudio>=2.6,<3.0",          # chatterbox restriction, find out why later
    "tqdm>=4.67,<5.0",
]
name = "gensay"
description = 'Multi-provider TTS tool compatible with macOS say command'
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
keywords = []
authors = [
    { name = "Anthony Wu", email = "pls-file-gh-issue@users.noreply.github.com" },
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Programming Language :: Python :: Implementation :: CPython",
]

[project.scripts]
gensay = "gensay.main:main"

[project.urls]
Documentation = "https://github.com/anthonywu/gensay#readme"
Issues = "https://github.com/anthonywu/gensay/issues"
Source = "https://github.com/anthonywu/gensay"

[project.optional-dependencies]
# Optional provider-specific dependencies
elevenlabs = ["elevenlabs[pyaudio]>=2.0,<3.0"]
# Audio format conversion
audio-formats = ["pydub>=0.25.0", "ffmpeg-python>=0.2.0"]
# All optional dependencies
all = ["gensay[elevenlabs,audio-formats]"]

# Development dependencies (PEP 735)
[dependency-groups]
dev = [
    "pdoc>=15.0,<16",
    "pygments>=2.19,<3.0",
    "pytest>=8.0.0",
    "pytest-cov>=5.0.0",
    # Note: ty is used via uvx, not installed as dependency
]


[tool.uv.sources]
# the upstream chatterbox-tts rigidly pins its dependencies, the fork relaxes the semvar ranges
chatterbox-tts = { git = "https://github.com/anthonywu/chatterbox.git", branch = "allow-dep-updates" }

[tool.pytest.ini_options]
filterwarnings = [
    # botocore uses deprecated datetime.utcnow() - waiting for upstream fix
    "ignore:datetime.datetime.utcnow:DeprecationWarning:botocore",
    # elevenlabs uses Pydantic V1 internals incompatible with Python 3.14+
    "ignore:Core Pydantic V1 functionality isn't compatible with Python 3.14:UserWarning",
    # pydub has invalid escape sequences in regex - waiting for upstream fix
    "ignore:.*is an invalid escape sequence:SyntaxWarning",
    # perth uses deprecated pkg_resources API
    "ignore:pkg_resources is deprecated:UserWarning",
    # diffusers uses deprecated LoRACompatibleLinear (PEFT recommended)
    "ignore:.*LoRACompatibleLinear.*is deprecated:FutureWarning",
]

[tool.coverage.run]
source_pkgs = ["gensay", "tests"]
branch = true
parallel = true
omit = ["src/gensay/__about__.py"]

[tool.coverage.paths]
gensay = ["src/gensay", "*/gensay/src/gensay"]
tests = ["tests", "*/gensay/tests"]

[tool.coverage.report]
exclude_lines = ["no cov", "if __name__ == .__main__.:", "if TYPE_CHECKING:"]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "C90", # mccabe complexity
    "UP",  # pyupgrade
    "ARG", # flake8-unused-arguments
    "SIM", # flake8-simplify
]
ignore = [
    "E501", # line too long (handled by formatter)
    "B008", # function calls in argument defaults
]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"] # unused imports in __init__ files
"tests/*" = ["ARG"]      # unused arguments in tests

# Type checking configuration for ty
[tool.ty.rules]
invalid-argument-type = "ignore"
invalid-assignment = "ignore"

# Note: ty is still in preview and doesn't support all mypy features yet
# The following settings were migrated from mypy but may not all be supported:
# - python_version = "3.11"
# - warn_return_any = true
# - warn_unused_configs = true
# - disallow_untyped_defs = true
# - disallow_incomplete_defs = true
# - check_untyped_defs = true
# - no_implicit_optional = true
# - warn_redundant_casts = true
# - warn_unused_ignores = true
# - warn_no_return = true
# - strict_optional = true
# - ignore_missing_imports for specific modules
