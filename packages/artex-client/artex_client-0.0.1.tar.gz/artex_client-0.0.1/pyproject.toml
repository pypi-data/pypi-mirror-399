[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "artex-client"
version = "0.0.1"
authors = [
    {name = "FoxVoland"},
    {name = "WeeNRizE"},
    {name = "ИЦИИ-МИФИ"}
]
description = "Клиентская часть фреймворка ARTEX"
requires-python = ">=3.9"
license = {text = "MIT"}

[tool.setuptools]
packages = ["artex_client"]