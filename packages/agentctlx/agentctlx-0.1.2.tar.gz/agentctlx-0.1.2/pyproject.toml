[project]
name = "agentctlx"
version = "0.1.2"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click>=8.3.1",
    "fastapi>=0.128.0",
    "rich>=14.2.0",
    "toml>=0.10.2",
    "twine>=6.2.0",
]

[project.scripts]
agentctlx = "agentctlx:cli"

[build-system]
requires = ["uv_build>=0.9.18,<0.10.0"]
build-backend = "uv_build"
