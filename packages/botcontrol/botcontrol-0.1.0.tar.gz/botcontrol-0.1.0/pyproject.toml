[project]
name = "botcontrol"
version = "0.1.0"
description = "Universal bot control engine"
authors = [{name="TM"}]
dependencies = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
