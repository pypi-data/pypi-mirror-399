[project]
name = "explainmath"
version = "0.1.0"
description = "A tiny library that catches invalid math operations and explains why they failed."
authors = [
    { name = "FraDevSae", email = "your@email.com" }
]
readme = "README.md"
license = "MIT"
requires-python = ">=3.8"

[tool.setuptools]
packages = ["core"]
