[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "hashemcolorful"
version = "0.1.0"
description = "Simple color helpers built on colorama"
readme = "README.md"
requires-python = ">=3.8"
dependencies = ["colorama"]

authors = [
  { name="Hashem Allaham" }
]
