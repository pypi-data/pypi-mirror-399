[project]
name = "pwchkr"
version = "0.1.0"
description = "Vérification de mot de passe avec score et détection de codes communs, adapté au français."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
