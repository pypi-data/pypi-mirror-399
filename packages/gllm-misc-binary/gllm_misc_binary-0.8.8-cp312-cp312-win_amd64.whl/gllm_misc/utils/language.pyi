def is_valid_language_code(language_code: str) -> bool:
    """Check if a string is a valid ISO 639-1 language code.

    Args:
        language_code (str): The language code to check.

    Returns:
        bool: True if the language code is a valid ISO 639-1 code, False otherwise.
    """
def get_language_name(language_code: str) -> str:
    """Get the name of a language from its ISO 639-1 code.

    Args:
        language_code (str): The ISO 639-1 code of the language.

    Returns:
        str: The name of the language.
    """
