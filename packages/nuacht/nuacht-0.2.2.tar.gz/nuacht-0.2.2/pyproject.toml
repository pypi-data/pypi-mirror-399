[build-system]
requires = ["setuptools >= 68.2.0"]
build-backend = "setuptools.build_meta"

[project]
name = "nuacht"
version = "0.2.2"
authors = [
  { name="Riain Ó Tuathail", email="rotuathail01@gmail.com" },
]
description = "Grabs entries from RSS feeds and stores in a database."
readme = "README.md"
requires-python = ">=3.9"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
license = "MIT"
license-files = ["LICEN[CS]E*"]
dependencies = [
    "feedparser==6.0.12",
    "PyYAML==6.0.3"
]