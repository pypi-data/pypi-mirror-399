[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mypackagemypackagemeta"
version = "1.0.1"
authors = [
	{name="Python learner", email="hestudio@hestudio.org"},
]
description = "A sample Python package"
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
	"requests>=2.25.0",
	"click"
]
