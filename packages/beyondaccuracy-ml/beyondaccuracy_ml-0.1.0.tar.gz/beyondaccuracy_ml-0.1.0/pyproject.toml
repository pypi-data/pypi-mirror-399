[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "beyondaccuracy-ml"
version = "0.1.0"
description = "Beyond Accuracy test package"
requires-python = ">=3.9"
