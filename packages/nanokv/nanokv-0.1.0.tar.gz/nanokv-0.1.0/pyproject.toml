[project]
name = "nanokv"
version = "0.1.0"
description = "A minimal key-value serialization format"
readme = "README.md"
authors = [{ name = "Tom Clesius", email = "tomclesius@gmail.com" }]
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
test = ["pytest>=8.0"]

[build-system]
requires = ["uv_build>=0.9.18,<0.10.0"]
build-backend = "uv_build"
