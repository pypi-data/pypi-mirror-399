# Read the Docs configuration file

version: 2

build:
  os: "ubuntu-20.04"
  tools:
    python: "3.10"

python:
  version: 3.10
  install:
    - method: pip
      path: .
      extra_requirements:
        - dev

sphinx:
  configuration: docs/conf.py

formats:
  - pdf
  - epub
