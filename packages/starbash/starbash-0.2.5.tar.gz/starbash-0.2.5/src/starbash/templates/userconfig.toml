# This is your (Starbash)[$PROJECT_URL] user configuration file.  It can be used to provide default settings
# to workflows.

# (or wherever is appropriate for the user's platform).

[repo]
kind = "preferences"

[tool]
# Starbash normally finds tools automatically via your PATH.  But if necessary you can define overrides here.  
# siril.path = "siril"
# siril.path = "flatpak run --command=siril-cli org.siril.Siril" # To use the flatpak version of siril

#[aliases]
# aliases can be used to map non standard (or non english) frame names to standard terms
# This is also used to map filters based on common misspellings or variations.
# We assume the first listed option in the list is the 'canonical' name used for printing etc...

# frame types
# dark = ["dark", "darks"]
# etc...

[analytics]
# enabled = true # default is true - change to false if you don't want any analytics/crash-reports
# include_user = false # default is false - change to true if you'd like your email added to crash reports/analytics

# [user]
# default author info
# name = "Your Name"
# email = "youremail@somedomain.com"
# website = "https://yourwebsite"
# license = "FIXME eventually applied as the default license for generated images Creative commons etc..."


# [config]
# Filters fits fields should we store in our DB cache.  By default we store all fits headers.
#fits-whitelist = [
#    "INSTRUME",
#    "FILTER",
#    "TELESCOP",
#    "IMAGETYP",
#    "DATE-OBS",
#    "DATE-LOC",
#    "DATE",
#    "EXPTIME",  # Use use this instead of EXPOSURE because it seems like not all apps use EXPOSURE (Siril)
#    "FWHEEL",
#    "OBJECT",
#    "RA",       # we ignore the text version OBJCTRA / OBJCTDEC
#    "DEC",
#    "OBJCTROT",
#    "FOCPOS",
#    "SITELAT",
#    "SITELON",
#    "SITEELEV",
#    "NAXIS1",
#    "NAXIS2",
#    "SWCREATE",
#    "XBINNING",
#    "YBINNING",
#    "GAIN",
#    "CCD-TEMP",
#    "SET-TEMP",
#    "AMBTEMP",
#]

# DO NOT edit below this line, they are managed automatically via
# the "sb repo add" etc... commands.
