[project]
name = "xhscreator-data-collection"
version = "1.0.4"
description = "XHS Creator Data Collection"
authors = [{ name = "martianbugs", email = "" }]
requires-python = ">=3.12"
dependencies = ["browser-automation-launcher>=1.0.7"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/XhscreatorDataCollection"]
