[build-system]
requires = ["setuptools>=69.0"]
build-backend = "setuptools.build_meta"

[project]
name = "nlp-toolkit-services"
version = "0.1.9"
description = "Python gRPC client stubs for the nlp services."
requires-python = ">=3.9"
dependencies = [
    "grpcio>=1.68.1",
    "protobuf>=5",
]

[project.urls]
Homepage = "https://github.com/citadel-reader/nlp-toolkit-services"
Repository = "https://github.com/citadel-reader/nlp-toolkit-services"

[tool.setuptools]
include-package-data = true
packages = ["nlp_toolkit_services_py_client"]
package-dir = { "nlp_toolkit_services_py_client" = "." }
