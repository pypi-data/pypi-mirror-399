# Руководство для контрибьюторов

## 📋 Содержание

- [Быстрый старт](#-быстрый-старт)
- [Окружение разработки](#-окружение-разработки)
- [Рабочий процесс](#-рабочий-процесс)
- [Стиль кода](#-стиль-кода)
- [Тестирование](#-тестирование)
- [Документация](#-документация)
- [Pull Request](#-pull-request)

## 🚀 Быстрый старт

```bash
# 1. Клонируйте форк
git clone https://github.com/YOUR_USERNAME/sandboxer-yandex-wiki-sync.git
cd sandboxer-yandex-wiki-sync

# 2. Установите зависимости
uv sync --all-extras

# 3. Проверьте установку
uv run pytest
```

## 🛠 Окружение разработки

### Требования

- **Python 3.11+**
- **uv** (рекомендуется) или pip
- **Git**

### Установка с uv

```bash
uv sync --all-extras
```

### Установка с pip

```bash
pip install -e ".[dev]"
```

## 🔄 Рабочий процесс

### 1. Создайте ветку

```bash
git checkout -b feature/my-feature
```

### 2. Проверьте изменения

```bash
# Линтер
uv run ruff check src/ tests/

# Типы
uv run mypy src/

# Тесты
uv run pytest
```

### 3. Формат сообщений коммитов

- `feat:` — новая функциональность
- `fix:` — исправление бага
- `docs:` — изменения в документации
- `test:` — добавление/изменение тестов
- `refactor:` — рефакторинг без изменения функциональности
- `chore:` — обновление зависимостей, CI и т.д.

## 🎨 Стиль кода

Используем **Ruff** для линтинга и форматирования.

```bash
# Автоисправление и форматирование
uv run ruff check --fix src/ tests/
uv run ruff format src/ tests/
```

### Type Hints

Все публичные функции должны иметь type hints.

```bash
uv run mypy src/
```

### Docstrings

Используйте Google-style docstrings.

## 🧪 Тестирование

```bash
# Все тесты с покрытием
uv run pytest --cov=src/sandboxer --cov-report=term-missing
```

## 📝 Документация

- **README.md**: Обновляйте при добавлении новых команд.
- **CHANGELOG.md**: Добавляйте запись в секцию `[Unreleased]`.

## 🔀 Pull Request

1. Убедитесь, что все тесты проходят.
2. Создайте Pull Request на GitHub.
3. Опишите изменения.
