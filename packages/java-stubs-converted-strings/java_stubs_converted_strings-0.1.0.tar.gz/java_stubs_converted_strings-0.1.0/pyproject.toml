[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "java-stubs-converted-strings"
version = "0.1.0"
description = "The JPype java.* type definitions with automatic Java string -> Python string conversions enabled"
readme = "README.rst"
license = "MIT"
requires-python = ">=3.11"
dependencies = [
    "jpype1>=1.6.0",
]

[project.urls]
Repository = "https://github.com/jstasiak/jpype-stubs"
"Bug Tracker" = "https://github.com/jstasiak/jpype-stubs/issues"
