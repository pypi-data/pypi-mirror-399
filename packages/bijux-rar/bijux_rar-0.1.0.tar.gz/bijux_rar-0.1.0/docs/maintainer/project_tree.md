STATUS: EXPLANATORY

# Project Tree

bijux-rar mirrors bijux-rag conventions.

**Repository guarantees:**
- `make all` runs real gates in CI.
- Tests are collected (non-zero).
- No filesystem pollution outside `artifacts/` and allowed tool caches.
- Placeholder OpenAPI drift gate exists (non-authoritative until API implementation).
