[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "coincity"
version = "0.1.0"
description = "Official Client for Coin City!"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.9"
authors = [
  { name = "Viren Sahti" }
]

dependencies = [
    "asyncio",
]

[project.scripts]
coincity = "coincity.main:main_sync"