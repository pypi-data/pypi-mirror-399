Show the current project status by running:

```bash
bpsai-pair status
```

Then read `.paircoder/context/state.md` and summarize:
1. Current plan and sprint
2. Active/in-progress tasks
3. Recent completions (last session)
4. What's next
