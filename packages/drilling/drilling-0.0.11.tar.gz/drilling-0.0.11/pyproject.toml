[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "drilling"          # nome no PyPI (único)
version = "0.0.11"
description = "Minha primeira lib no PyPI"
readme = "README.md"
authors = [
  { name="Isaac Farias", email="zackfarias.work@email.com" }
]
license = { text = "MIT" }
requires-python = ">=3.9"
dependencies = [
    "pandas>=2.0,<3.0",
    "SQLAlchemy>=1.4,<3",
    "paramiko>=3.4,<4",
    "sshtunnel>=0.4.0"
]

[project.urls]
Homepage = "https://github.com/seuuser/minha-lib-top"
