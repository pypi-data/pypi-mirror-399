[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tkeditor"
version = "0.1.1"
description = "A Python editor with syntax highlighting, folding, line numbers, and bracket tracking"
readme = "README.md"
requires-python = ">=3.9"

authors = [
    { name = "Anmol Kumar", email = "ask995844@gmail.com" }
]

license = { file = "LICENSE" }

dependencies = []

[project.urls]
Homepage = "https://github.com/TechRuler/tkeditor"
