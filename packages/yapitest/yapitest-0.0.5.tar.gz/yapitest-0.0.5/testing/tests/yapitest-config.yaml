# List of default variables that can be used in all tests in this project
vars:
  default-url:
    env: BASE_URL
    default: http://127.0.0.1:8181
  sample-user:
    env: SAMPLE_USERNAME
    default: test-user-123
  sample-password:
    env: SAMPLE_PASSWORD
    default: p4ssw0rd123!

urls:
  default: $vars.default-url

