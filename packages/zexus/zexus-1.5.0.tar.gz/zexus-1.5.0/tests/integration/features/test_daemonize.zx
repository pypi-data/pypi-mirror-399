# test_daemonize.zx - Test daemon process creation

print("Testing daemonize()...")

# Create a daemon that writes to a file
if is_main() {
    print("Creating daemon process...")
    
    # This would daemonize the process
    # Note: In testing, we skip actual daemonization to avoid detaching
    # from the test environment
    
    # In production use:
    # daemonize()
    # daemonize("/var/run/myapp")  # With custom working directory
    
    print("✅ daemonize() function is available")
    print("   (Not executing to avoid detaching from terminal)")
    print("")
    print("Production usage:")
    print("  daemonize()                    # Daemonize with default settings")
    print("  daemonize('/var/run/myapp')    # Daemonize with custom working dir")
}

print("Done")
