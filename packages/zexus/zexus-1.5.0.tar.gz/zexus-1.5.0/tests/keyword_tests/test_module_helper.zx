// Test module for import
// File: test_module_helper.zx

action greet(name) {
    return "Hello, " + name;
}

action add(a, b) {
    return a + b;
}

const PI = 3.14159;

let counter = 0;

action incrementCounter() {
    counter = counter + 1;
    return counter;
}

// Export statement (if supported)
export greet;
export add;
export PI;
