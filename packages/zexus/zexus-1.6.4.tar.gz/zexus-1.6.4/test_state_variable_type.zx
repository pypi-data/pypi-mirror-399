contract Consensus {
    state min_stake = 1000
    
    action register_validator(stake) {
        print("min_stake value: " + string(min_stake))
        print("stake value: " + string(stake))
        
        if stake >= min_stake {
            print("✓ Stake is sufficient")
            return true
        } else {
            print("✗ Stake too low")
            return false
        }
    }
}

let consensus = Consensus{}
print("Test 1: stake=1500, should pass")
consensus.register_validator(1500)

print("")
print("Test 2: stake=500, should fail")
consensus.register_validator(500)
