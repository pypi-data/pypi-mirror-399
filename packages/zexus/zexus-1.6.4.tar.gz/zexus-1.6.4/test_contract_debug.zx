contract SimpleToken {
    state total_supply = 1000000
    state balances = {}
    
    action init() {
        balances["genesis"] = total_supply
        print("Token initialized with supply: " + string(total_supply))
        print("Genesis balance: " + string(balances["genesis"]))
    }
}

let token = SimpleToken()
token.init()
